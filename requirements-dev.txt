# Test-only deps. At runtime langchain-core + pyyaml are provided by the host
# (protoAgent); these let the suite run standalone in CI with no host present.
# The plugin itself adds NO runtime pip deps.
langchain-core>=0.2
pyyaml>=6
pytest>=8
